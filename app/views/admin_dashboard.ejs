<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Admin - JB Motor</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body { min-height: 100vh; overflow-x: hidden; }
        .sidebar { min-height: 100vh; background-color: #212529; color: white; }
        .sidebar a { color: #adb5bd; text-decoration: none; padding: 10px 20px; display: block; }
        .sidebar a:hover, .sidebar a.active { background-color: #0d6efd; color: white; }
        .main-content { padding: 20px; }
    </style>
</head>
<body>
    <div class="d-flex">
        <div class="sidebar col-md-2 d-none d-md-block">
            <div class="p-3 text-center border-bottom border-secondary">
                <h4 class="fw-bold">🛡️ Admin Panel</h4>
                <small>Halo, <%= user.nama %></small>
            </div>
            <ul class="list-unstyled mt-3">
                <li><a href="/admin/dashboard" class="active"><i class="bi bi-speedometer2 me-2"></i> Dashboard</a></li>
                <li><a href="/admin/katalog"><i class="bi bi-car-front-fill me-2"></i> Daftar Kendaraan</a></li>
                <li><a href="/admin/upload"><i class="bi bi-cloud-upload-fill me-2"></i> Upload Unit Baru</a></li>
                <li><a href="/admin/logs"><i class="bi bi-journal-text me-2"></i> Log Penjualan</a></li>
                <li class="mt-5"><a href="/logout" class="text-danger"><i class="bi bi-box-arrow-left me-2"></i> Logout</a></li>
            </ul>
        </div>

        <div class="col-md-10 w-100 main-content bg-light">
            <nav class="navbar navbar-light bg-white shadow-sm mb-4 d-md-none">
                <div class="container-fluid">
                    <span class="navbar-brand">Admin Panel</span>
                    <a href="/logout" class="btn btn-sm btn-danger">Logout</a>
                </div>
            </nav>

            <h2 class="mb-4">Statistik Kunjungan Website 📈</h2>

            <div class="btn-group mb-4">
                <a href="/admin/dashboard?days=1" class="btn btn-outline-primary <%= activeRange == 1 ? 'active' : '' %>">Hari Ini</a>
                <a href="/admin/dashboard?days=7" class="btn btn-outline-primary <%= activeRange == 7 ? 'active' : '' %>">7 Hari Terakhir</a>
                <a href="/admin/dashboard?days=30" class="btn btn-outline-primary <%= activeRange == 30 ? 'active' : '' %>">30 Hari Terakhir</a>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <canvas id="visitChart" style="max-height: 400px;"></canvas>
                </div>
            </div>

            <div class="row mt-4">
                <div class="col-md-4">
                    <div class="card bg-primary text-white">
                        <div class="card-body">
                            <h5>Total Kunjungan (Range Ini)</h5>
                            <h3><%= JSON.parse(chartData).reduce((a, b) => a + b, 0) %> Views</h3>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        const ctx = document.getElementById('visitChart').getContext('2d');
        const labels = <%- chartLabels %>; // Data dari Controller
        const data = <%- chartData %>;     // Data dari Controller

        new Chart(ctx, {
            type: 'line',
            data: {
                labels: labels,
                datasets: [{
                    label: 'Jumlah Pengunjung',
                    data: data,
                    borderColor: '#0d6efd',
                    backgroundColor: 'rgba(13, 110, 253, 0.1)',
                    borderWidth: 2,
                    fill: true,
                    tension: 0.4 // Garis melengkung halus
                }]
            },
            options: {
                responsive: true,
                plugins: {
                    legend: { position: 'top' },
                },
                scales: {
                    y: { beginAtZero: true }
                }
            }
        });
    </script>
</body>
</html>