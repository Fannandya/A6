<!DOCTYPE html>
<html lang="id">
<head>
    <title>Edit Motor - JB Motor</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5 mb-5">
        <div class="card shadow">
            <div class="card-header bg-warning text-dark">
                <h4 class="mb-0">✏️ Edit Data Kendaraan</h4>
            </div>
            <div class="card-body">
                <form action="/admin/edit/<%= motor.id %>" method="POST" enctype="multipart/form-data">
                    <input type="hidden" name="oldGambar" value="<%= motor.gambar %>">

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Merek</label>
                            <input type="text" name="merek" class="form-control" value="<%= motor.merek %>" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Model</label>
                            <input type="text" name="model" class="form-control" value="<%= motor.model %>" required>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Tahun</label>
                            <input type="number" name="tahun" class="form-control" value="<%= motor.tahun %>" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label">CC Mesin</label>
                            <input type="number" name="cc" class="form-control" value="<%= motor.cc %>" required>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Harga</label>
                        <input type="number" name="harga" class="form-control" value="<%= motor.harga %>" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Deskripsi</label>
                        <textarea name="deskripsi" class="form-control" rows="3"><%= motor.deskripsi %></textarea>
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label">Ganti Foto? (Biarkan kosong jika tidak ingin ganti)</label>
                        <div class="d-flex align-items-center gap-3">
                            <img src="/uploads/<%= motor.gambar || 'default.jpg' %>" width="80" class="rounded border">
                            <input type="file" name="gambar" class="form-control">
                        </div>
                    </div>

                    <a href="/admin/katalog" class="btn btn-secondary">Batal</a>
                    <button type="submit" class="btn btn-primary">Simpan Perubahan</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>