<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JB Motor Jogja - Jual Beli Motor Aman</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        .hero-section { background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('https://images.unsplash.com/photo-1558981403-c5f9899a28bc?auto=format&fit=crop&w=1920&q=80'); background-size: cover; background-position: center; color: white; padding: 100px 0; }
        .card-img-top { height: 200px; object-fit: cover; }
        .hover-effect:hover { transform: translateY(-5px); transition: 0.3s; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">🏍️ JB Motor Jogja</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link text-warning fw-bold" href="https://api.whatsapp.com/send?phone=6281234567890&text=Halo%20Admin,%20saya%20mau%20titip%20jual%20motor" target="_blank">
                            💰 Jual Motor
                        </a>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link fw-bold" href="/testimoni">💬 Testimoni</a>
                    </li>
                    
                    <% if (user) { %>
                        <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard Saya</a></li>
                        <li class="nav-item"><a class="nav-link btn btn-outline-danger btn-sm text-white ms-2" href="/logout">Logout</a></li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link" href="/login">Masuk</a></li>
                        <li class="nav-item"><a class="nav-link btn btn-primary btn-sm text-white ms-2" href="/register">Daftar</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <header class="hero-section text-center">
        <div class="container">
            <h1 class="display-4 fw-bold">Cari Motor Bekas Berkualitas?</h1>
            <p class="lead">Platform titip jual motor terpercaya di Yogyakarta.</p>
        </div>
    </header>

    <div class="container mt-5 mb-5">
        <h3 class="text-center mb-4 border-bottom pb-2">Daftar Motor Tersedia</h3>
        
        <div class="row">
            <% if (products.length > 0) { %>
                <% products.forEach(motor => { %>
                    <% 
                        // --- LOGIKA JAVASCRIPT ---
                        // 1. Format Nomor HP (08xx -> 628xx)
                        let noHp = motor.no_hp || '6281234567890'; 
                        if (noHp.startsWith('0')) { 
                            noHp = '62' + noHp.slice(1); 
                        }

                        // 2. Cek Gambar (Kalau null, pakai default)
                        let gambarMotor = motor.gambar;
                        if (!gambarMotor || gambarMotor === 'null') {
                            gambarMotor = 'default.jpg';
                        }
                    %>

                    <div class="col-md-4 mb-4">
                        <div class="card h-100 shadow-sm hover-effect">
                            <img src="/uploads/<%= gambarMotor %>" class="card-img-top" alt="<%= motor.merek %>" onerror="this.src='/uploads/default.jpg'">
                            
                            <div class="card-body">
                                <h5 class="card-title"><%= motor.merek %> <%= motor.model %></h5>
                                <p class="text-muted mb-1"><%= motor.tahun %> • <%= motor.cc %> CC</p>
                                <p class="text-muted small">Penjual: <%= motor.nama_penjual %></p> 
                                <h4 class="text-primary fw-bold">Rp <%= parseInt(motor.harga).toLocaleString('id-ID') %></h4>
                                <p class="card-text text-truncate"><%= motor.deskripsi %></p>
                            </div>
                            <div class="card-footer bg-white border-top-0 d-grid">
                                <a href="https://api.whatsapp.com/send?phone=<%= noHp %>&text=Halo%20Kak%20<%= motor.nama_penjual %>,%20saya%20tertarik%20dengan%20motor%20<%= motor.merek %>%20<%= motor.model %>" target="_blank" class="btn btn-success">
                                    <i class="bi bi-whatsapp"></i> Hubungi Penjual
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-12 text-center py-5">
                    <h4 class="text-muted">Belum ada motor yang dijual saat ini.</h4>
                    <p>Coba upload unit baru lewat Dashboard Admin!</p>
                </div>
            <% } %>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-3 mt-auto">
        <small>&copy; 2026 JB Motor Jogja - Kelompok A6</small>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>