<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JB Motor Jogja - Jual Beli Motor Bekas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        .hero-section {
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('https://images.unsplash.com/photo-1558981403-c5f9899a28bc?ixlib=rb-1.2.1&auto=format&fit=crop&w=1350&q=80');
            background-size: cover;
            background-position: center;
            color: white;
            min-height: 600px;
            position: relative;
            display: flex;
            align-items: center;
        }
        
        .hero-content { width: 100%; }

        /* Styling Kotak Pencarian */
        .search-card {
            background: rgba(255, 255, 255, 0.95);
            padding: 25px;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.3);
            margin-top: 30px;
            max-width: 750px;
            border-left: 5px solid #0d6efd;
        }

        .card-img-top {
            height: 200px;
            object-fit: cover;
        }
        .hover-effect { transition: transform 0.2s; }
        .hover-effect:hover { transform: translateY(-5px); }
    </style>
</head>
<body class="bg-light">

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">🏍️ JB Motor Jogja</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item"><a class="nav-link active" href="/">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/testimoni">Testimoni</a></li>
                    <% if (user) { %>
                        <li class="nav-item ms-2"><a class="nav-link btn btn-outline-light btn-sm px-3" href="/dashboard">Dashboard</a></li>
                    <% } else { %>
                        <li class="nav-item ms-2"><a class="nav-link" href="/login">Masuk</a></li>
                        <li class="nav-item ms-1"><a class="btn btn-primary btn-sm px-3 rounded-pill" href="/register">Daftar</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <header class="hero-section">
        <div class="container hero-content">
            <div class="text-start mb-2">
                <h1 class="display-4 fw-bold">Cari Motor Impianmu?</h1>
                <p class="lead text-white-50">Pusat jual beli motor bekas berkualitas di Jogja. Aman & Terpercaya.</p>
            </div>

            <div class="search-card text-dark text-start">
                <h5 class="mb-3 fw-bold border-bottom pb-2">
                    <i class="bi bi-search"></i> Cari spesifikasi yang anda inginkan...
                </h5>
                <form action="/" method="GET">
                    <div class="row g-3 align-items-end">
                        <div class="col-md-5">
                            <label class="form-label fw-bold small text-secondary">Merek Motor</label>
                            <input type="text" name="searchMerek" class="form-control form-control-lg" 
                                   placeholder="Yamaha, Honda..." value="<%= searchMerek %>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-bold small text-secondary">Kapasitas Mesin (CC)</label>
                            <input type="number" name="searchCC" class="form-control form-control-lg" 
                                   placeholder="150, 250..." value="<%= searchCC %>">
                        </div>
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-primary btn-lg w-100 fw-bold">
                                <i class="bi bi-search"></i> Cari
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </header>

    <div class="container mb-5 mt-5" id="katalog">
        <div class="text-center mb-5">
            <h2 class="fw-bold">Daftar Motor Tersedia</h2>
            <div class="d-flex justify-content-center">
                <div style="height: 4px; width: 60px; background-color: #0d6efd; border-radius: 2px;"></div>
            </div>
        </div>

        <div class="row">
            <% if (products.length > 0) { %>
                <% products.forEach(motor => { %>
                    <div class="col-md-3 mb-4">
                        <div class="card h-100 shadow-sm border-0 hover-effect">
                            <img src="/uploads/<%= motor.gambar || 'default.jpg' %>" 
                                 class="card-img-top" alt="<%= motor.merek %>"
                                 onerror="this.src='/uploads/default.jpg'">
                            
                            <div class="card-body">
                                <h5 class="card-title fw-bold"><%= motor.merek %> <%= motor.model %></h5>
                                <p class="card-text text-muted small mb-2">
                                    <i class="bi bi-calendar-event"></i> <%= motor.tahun %> &nbsp;•&nbsp; 
                                    <i class="bi bi-fuel-pump"></i> <%= motor.cc %> CC
                                </p>
                                <h5 class="text-primary fw-bold mb-3">Rp <%= parseInt(motor.harga).toLocaleString('id-ID') %></h5>
                                <p class="card-text small text-secondary">
                                    <%= motor.deskripsi ? motor.deskripsi.substring(0, 50) + '...' : 'Kondisi mulus.' %>
                                </p>
                            </div>

                            <div class="card-footer bg-white border-top-0 pb-3">
                                
                                <a href="https://wa.me/6282220231617?text=Halo%20Admin,%20saya%20tertarik%20dengan%20motor%20<%= motor.merek %>%20<%= motor.model %>%20Tahun%20<%= motor.tahun %>%20(<%= motor.cc %>%20CC)%20seharga%20Rp%20<%= parseInt(motor.harga).toLocaleString('id-ID') %>" 
                                   target="_blank" class="btn btn-success w-100 rounded-pill fw-bold">
                                    <i class="bi bi-whatsapp"></i> Hubungi Admin
                                </a>

                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <% if (!notFound) { %>
                    <div class="col-12 text-center py-5">
                        <h3 class="text-muted">Belum ada motor yang tersedia.</h3>
                    </div>
                <% } %>
            <% } %>
        </div>
    </div>

    <div class="modal fade" id="notFoundModal" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content text-center p-4">
                <div class="modal-body">
                    <i class="bi bi-search display-1 text-primary mb-3 d-block"></i>
                    <h3 class="fw-bold mb-3">Waduh, Motornya Gak Ada!</h3>
                    <p class="text-muted mb-4">
                        Kami tidak menemukan motor dengan Merek <strong>"<%= searchMerek %>"</strong> 
                        dan Kapasitas <strong>"<%= searchCC %>"</strong> CC.
                    </p>
                    <a href="/" class="btn btn-primary btn-lg rounded-pill px-5">Kembali ke Homepage</a>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-3 mt-5">
        <div class="container">
            <small>&copy; 2026 JB Motor Jogja - Kelompok A6</small>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <% if (notFound) { %>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            var myModal = new bootstrap.Modal(document.getElementById('notFoundModal'));
            myModal.show();
        });
    </script>
    <% } %>

</body>
</html>