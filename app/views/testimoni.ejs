<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Testimoni - JB Motor Jogja</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>
<body class="bg-light">

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">🏍️ JB Motor Jogja</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    
                    <% if (!user) { %>
                        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                    <% } %>

                    <li class="nav-item"><a class="nav-link active fw-bold" href="/testimoni">Testimoni</a></li>
                    
                    <% if (user) { %>
                        <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link btn btn-primary btn-sm text-white ms-2" href="/login">Masuk</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-5 mb-5">
        <h2 class="text-center mb-4">Apa Kata Mereka? 💬</h2>

        <div class="card shadow-sm mb-5">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0">Tulis Pengalamanmu</h5>
            </div>
            <div class="card-body">
                <form action="/testimoni" method="POST">
                    <div class="row">
                        <div class="col-md-4 mb-3">
                            <label class="form-label">Nama Kamu</label>
                            <input type="text" name="nama" class="form-control" placeholder="Isi nama..." value="<%= user ? user.nama : '' %>" required>
                        </div>
                        <div class="col-md-8 mb-3">
                            <label class="form-label">Ceritakan Pengalamanmu</label>
                            <textarea name="isi" class="form-control" rows="2" placeholder="Pelayanan ramah, motor mulus banget..." required></textarea>
                        </div>
                    </div>
                    <% if (user) { %>
                        <button type="submit" class="btn btn-success"><i class="bi bi-send"></i> Kirim Testimoni</button>
                    <% } else { %>
                        <a href="/login" class="btn btn-warning">Login untuk Mengirim</a>
                    <% } %>
                </form>
            </div>
        </div>

        <div class="row">
            <% if (testimonials.length > 0) { %>
                <% testimonials.forEach(t => { %>
                    <div class="col-md-6 mb-4">
                        <div class="card h-100 shadow-sm">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-start mb-2">
                                    <div>
                                        <h5 class="card-title fw-bold text-primary mb-0"><i class="bi bi-person-circle"></i> <%= t.nama %></h5>
                                        <small class="text-muted" style="font-size: 0.8rem;"><%= new Date(t.created_at).toLocaleDateString('id-ID') %></small>
                                    </div>
                                    
                                    <% if (user && (user.id === t.user_id || user.role === 'admin')) { %>
                                        <div class="d-flex gap-1">
                                            <a href="/testimoni/edit/<%= t.id %>" class="btn btn-sm btn-outline-primary" title="Edit">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <form action="/testimoni/delete/<%= t.id %>" method="POST" onsubmit="return confirm('Hapus testimoni ini?');">
                                                <button type="submit" class="btn btn-sm btn-outline-danger" title="Hapus">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            </form>
                                        </div>
                                    <% } %>
                                </div>
                                <hr class="my-2">
                                <p class="card-text fst-italic">"<%= t.isi %>"</p>
                            </div>
                            
                            <div class="card-footer bg-white d-flex gap-2">
                                <form action="/testimoni/<%= t.id %>/react" method="POST" class="d-inline">
                                    <input type="hidden" name="type" value="like">
                                    <button type="submit" class="btn btn-light btn-sm text-success border">
                                        <i class="bi bi-hand-thumbs-up-fill"></i> <%= t.likes %>
                                    </button>
                                </form>
                                <form action="/testimoni/<%= t.id %>/react" method="POST" class="d-inline">
                                    <input type="hidden" name="type" value="dislike">
                                    <button type="submit" class="btn btn-light btn-sm text-danger border">
                                        <i class="bi bi-hand-thumbs-down-fill"></i> <%= t.dislikes %>
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-12 text-center text-muted">
                    <p>Belum ada testimoni. Jadilah yang pertama menulis!</p>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>